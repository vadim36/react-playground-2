export enum ButtonVariants {
  primary = 'primary',
  danger = 'danger'
}

export enum SortOptions {
  default = '',
  title = 'title',
  body = 'body'
}

export enum Sizes {
  large = 'lg',
  medium = 'md',
  small = 'sm'
}

export enum Routes {
  posts = '/posts',
  about = '/about',
  post = '/posts/:id',
  any = '*'
}